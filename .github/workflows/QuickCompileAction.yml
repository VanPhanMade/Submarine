name: QuickCompileWorkflow

on:
  workflow_dispatch:

jobs:
  script-compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug
        run: echo "✅ Script compile job started"

      - name: Unity Script Compile Check (editmode, license-free)
        id: unity
        uses: game-ci/unity-test-runner@v4
        continue-on-error: true   # 👈 don't fail immediately
        with:
          projectPath: .
          testMode: editmode

      # Save Unity logs as an artifact
      - name: Upload Unity Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Unity-Logs
          path: artifacts

      # Summarize compile results at the very end
      - name: Summarize Compile Results
        if: always()
        run: |
          if [ "${{ steps.unity.outcome }}" != "success" ]; then
            echo "❌ Unity compile errors were detected. Check the Unity-Logs artifact."
            exit 1
          else
            echo "✅ Unity compiled successfully."
          fi
  # --------------------------------------------------------------------
  # build-compile:
  #   runs-on: ubuntu-latest
  #   needs: script-compile
  #   env:
  #     UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
  #     UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
  #     UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Unity Build Check
  #       uses: game-ci/unity-builder@v4
  #       with:
  #         targetPlatform: StandaloneLinux64
  #         buildMethod: ""
  #
  # NOTE:
  # This requires a valid Unity license (Pro/Enterprise or properly
  # activated Personal license with UNITY_LICENSE secret). Uncomment
  # once you are ready to run full builds in CI.
  # --------------------------------------------------------------------
